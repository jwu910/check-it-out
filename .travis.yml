language: node_js
cache:
  directories:
    - "node_modules"
node_js:
  - "node"
  - "lts/*"
script:
  - npm run build
  - node index.js -v
  - npm test
  - npm run lint
jobs:
  include:
    - stage: danger
      node_js:
        - "node"
      script: yarn danger ci
